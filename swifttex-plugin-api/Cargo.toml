[package]
name = "swifttex-plugin-api"
version = "0.4.0"
edition = "2021"
license = "MIT"
description = "Plugin system APIs for SwiftTeX."

[dependencies]

[dev-dependencies]
swifttex-parser = { path = "../swifttex-parser" }
